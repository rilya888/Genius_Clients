name: Deploy Worker Branch Sync

on:
  push:
    branches:
      - main
    paths:
      - "apps/worker/**"
      - "packages/db/**"
      - "packages/shared/**"
      - "scripts/release/sync-service-branch.sh"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/deploy-worker-branch-sync.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-worker-branch:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Sync deploy/worker from main
        run: bash scripts/release/sync-service-branch.sh worker deploy main
