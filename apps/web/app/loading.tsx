export default function GlobalLoading() {
  return (
    <main className="gc-system-page">
      <section className="gc-card gc-system-card">
        <h1 className="gc-system-title">Loading</h1>
        <p className="gc-system-text">Please wait while we prepare the page.</p>
      </section>
    </main>
  );
}
