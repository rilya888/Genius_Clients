# Migrations

Use `pnpm --filter @genius/db db:generate` to generate migration files.
Use `pnpm --filter @genius/db db:migrate` to apply generated migrations.

Stage 02 implementation will add the first generated SQL migration.
